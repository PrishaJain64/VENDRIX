<% layout('layouts/specifications-boilerplate') %>
<div id="cur-dot"></div>
<div id="cur-ring"></div>
<!-- LIGHTBOX -->
<div class="lightbox-overlay" id="lightbox" onclick="closeLightbox(event)">
  <div class="lightbox-inner">
    <button class="lightbox-close" onclick="closeLightbox()"><i class="fas fa-times"></i></button>
    <img class="lightbox-img" id="lightboxImg" src="" alt="">
  </div>
</div>

<!-- TOAST -->
<div class="toast" id="toast">
  <i class="fas fa-exclamation-circle"></i>
  <span class="toast-text"></span>
</div>

<!-- TOP BAR -->
<div class="topbar">
  <div class="topbar-track">
    <span class="topbar-item">Free Shipping Over ₹2,000</span>
    <span class="topbar-item">1 Year Warranty Included</span>
    <span class="topbar-item">Certified Refurbished Devices</span>
    <span class="topbar-item">Easy EMI Essentials</span>
    <span class="topbar-item">Expert Repair Services</span>
    <span class="topbar-item">All Devices Available</span>
    <span class="topbar-item">Free Shipping Over ₹2,000</span>
    <span class="topbar-item">1 Year Warranty Included</span>
    <span class="topbar-item">Certified Refurbished Devices</span>
    <span class="topbar-item">Easy EMI Essentials</span>
    <span class="topbar-item">Expert Repair Services</span>
    <span class="topbar-item">All Devices Available</span>
  </div>
</div>

<!-- NAVBAR -->
<header class="v-navbar" id="navbar">
  <a href="#" class="logo">Vend<span class="accent">r</span>ix</a>
  <%- include('../partials/navbar.ejs') %>
  <div class="nav-right">
    <a href="/vendrix/aboutUs" class="nav-about">About Us</a>
    <%if(currentUser){%>
      <a href="/vendrix/profile"><span class="reviewer-avatar"><%=currentUser.firstname.slice(0,1)%></span></a>
    <%}else{%>
      <a href="/vendrix/register"><span class="nav-cta">Sign Up/In</span></a>
  <%}%>
    <a href="/vendrix/shoppingcart" class="nav-cta"><i class="fas fa-shopping-bag"></i> Cart</a>
    <a href="/buy/<%=spe.type%>" class="nav-cta"><i class="fas fa-arrow-left"></i> Back</a>
  </div>
</header>

<!-- BREADCRUMB -->
<div class="breadcrumb-bar">
  <a href="/vendrix">Home</a><span class="sep">›</span>
  <a href="/buy/<%=spe.type%>">Buy</a><span class="sep">›</span>
  <span class="cur"><%=spe.name%></span>
</div>

<main class="product-page">

  <!-- ══ LEFT: GALLERY PANEL ══ -->
   <div class="gallery-panel" id="galleryPanel">

  <!-- ══ BOOTSTRAP CAROUSEL ══ -->
  <div id="productCarousel" class="carousel slide" data-bs-ride="false">

    <!-- indicators -->
    <div class="carousel-indicators" id="bsIndicators">
      <button 
        type="button" 
        data-bs-target="#productCarousel" 
        data-bs-slide-to="0" 
        class="active"
        aria-current="true"
        aria-label="Slide 1">
      </button>

      <% for(let i in spe.colors[color_key].images){ 
           let val = Number(i); %>

        <button 
          type="button" 
          data-bs-target="#productCarousel" 
          data-bs-slide-to="<%= val + 1 %>" 
          aria-label="<%= 'Slide ' + (val + 2) %>">
        </button>

      <% } %>
    </div>

    <div class="carousel-inner" id="bsInner">

      <div class="carousel-item active">
        <img src="<%= spe.colors[color_key].thumbnail.url %>" class="d-block w-50 mx-auto">
      </div>

      <% for(let val of spe.colors[color_key].images){ %>
        <div class="carousel-item">
          <img src="<%= val.url %>" class="d-block w-50 mx-auto">
        </div>
      <% } %>

    </div>

    <button class="carousel-control-prev" type="button" data-bs-target="#productCarousel" data-bs-slide="prev">
      <div class="v-arrow"><i class="fas fa-chevron-left"></i></div>
      <span class="visually-hidden">Previous</span>
    </button>

    <button class="carousel-control-next" type="button" data-bs-target="#productCarousel" data-bs-slide="next">
      <div class="v-arrow"><i class="fas fa-chevron-right"></i></div>
      <span class="visually-hidden">Next</span>
    </button>

  </div>

</div>

  <div class="info-panel" id="infoPanel">

    <div class="product-eyebrow reveal"><span class="eyebrow-line"></span><span class="eyebrow-tag"><%=spe.type%></span></div>
    <div class="product-brand-label reveal"><%=spe.brand%></div>
    <h1 class="product-title reveal"><%=spe.name%></h1>

    <div class="rating-row reveal rd1">
      <span class="stars-og" id="stars-og">★★★★★</span>
      <span class="rating-num" id="rating-num">4.8</span>
      <span class="rating-count">· <%=reviews.length%> reviews</span>
    </div>

    <div class="price-block reveal rd1">
      <div class="price-label">Starting Price</div>
      <span class="price-value">INR</span>
      <div style ="display:inline" class="price-value" id="priceDisplay"><%=spe.variants[i].price%></div>
    </div>
    <form>
    <div class="option-group reveal rd2">
      <div class="variant-grid">
        <%for(let [r,md] of spe.variants.entries()){%>
        <input type ="submit" formaction="/buydetails/<%= spe._id %>/<%= r %>/<%=color_key%>" class="variant-btn <%= r == i ? 'selected' : '' %>" value = "<%=md.label%>" />
        <%}%>
      </div>
    </div>
    </form>
    <form>
    <div class="option-group reveal rd2" style="margin-bottom:32px;">
      <div class="option-label">Colour — <span class="chosen" id="selectedColor"><%=spe.colors[color_key].color%></span></div>
      <div class="color-swatches">
        <%for(let [k,md] of spe.colors.entries()){%>
            <input type = "submit" class="color-swatch <%= k == color_key ? 'selected' : '' %>" style ="background: <%=md.hexcode%>" formaction="/buydetails/<%= spe._id %>/<%= i %>/<%=k%>" value=""/>
        <%}%>
      </div>
    </div>
    </form>

        <div class="qty-row reveal rd3">
      <span class="qty-label">Qty</span>
      <div class="qty-ctrl">
        <button type="button" class="qty-btn minus"><i class="fas fa-minus" style="font-size:11px"></i></button>
        <div class="qty-val" id="qtyVal"><%=spe.quantity?spe.quantity:1%></div>
        <button type="button" class="qty-btn plus"><i class="fas fa-plus" style="font-size:11px"></i></button>
      </div>
    </div>
    <form id="form1">
    <div class="action-row reveal rd3">
      <button type="button" id="cartBtn" onclick = "addtoCart('buy','<%=spe._id%>','<%=i%>','<%=color_key%>')"  class="btn-cart btn-primary">
         <span class="label-default">
        <i class="fas fa-shopping-bag"></i><span id="cartBtnText">Add to Cart</span>
        </span>
        <span class="label-success">
        <svg class="tick-circle" width="26" height="26" viewBox="0 0 26 26">
          <circle cx="13" cy="13" r="11"/>
          <polyline points="7,13 11,17 19,9"/>
        </svg>
        ADDED!
      </span>
      </button>
    </div>
    </form>
    <div class="action-row reveal rd3" style="margin-top:-6px;">
      <button id="buy" class="btn-buy"><i class="fas fa-bolt"></i> Buy Now</button>
    </div>

    <div class="trust-strip reveal rd4">
      <div class="trust-it"><i class="fas fa-shipping-fast"></i><span>Free Delivery</span></div>
      <div class="trust-it"><i class="fas fa-shield-alt"></i><span>1 Year Warranty</span></div>
      <div class="trust-it"><i class="fas fa-undo"></i><span>10-Day Returns</span></div>
    </div>

    <div class="divider"></div>

    <%#specification%>
    <div class="specs-section reveal">
      <div class="specs-header" id="specsHeader">
        <div class="specs-title">Full Specifications</div>
        <div class="specs-toggle"><i class="fas fa-minus" id="specsToggleIcon"></i></div>
      </div>
      <div id="specsBody">
        <%for(let [key,value] of Object.entries(spe.specifications)){%>
        <div class="spec-row"><div class="spec-key"><%=key%></div><div class="spec-val"><%=value%>t</div></div>
        <%}%>
      </div>
    </div>

  <%- include('../partials/review.ejs') %>
  </div>

</main>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
<script src = "/js/specification.js"></script>
<script src = "/js/counter.js"></script>
<script src = "/js/reload.js"></script>
<script src = "/js/reviews.js"></script>

<script>
  const star = '<%=star%>'||null;
  const select = '<%=option%>' ||'relevant';
  const num = Number('<%=reviews.length%>');
  activeStarFilter = star;
  const isFiltering = activeStarFilter !== null || select !== 'relevant';

  const clearBtn = document.getElementById('filterClearBtn');
  const indicator = document.getElementById('activeFilterIndicator');
  const list = document.getElementById('reviewsList');
  const emptyState = document.getElementById('reviewsEmpty');

  clearBtn.classList.toggle('visible', isFiltering);

  if(activeStarFilter && activeStarFilter !=='null'){
  const btn = document.querySelector(`button[data-star="${activeStarFilter}"]`);
  
  document.querySelectorAll('.star-chip').forEach(c => c.classList.remove('active')); 
  btn.classList.add('active');
  indicator.classList.add('visible');
    document.getElementById('activeFilterText').textContent =
    'Showing ' + num + ' review' + (num >= 1 ? 's' : '') + ' with ' + activeStarFilter + '-star rating';
  }
  else { indicator.classList.remove('visible'); }


  document.getElementById('sortSelect').value = select;


  if (num === 0) {
    list.innerHTML = ''; emptyState.classList.add('visible'); loadMoreWrap.style.display = 'none';
  } else {
    emptyState.classList.remove('visible'); loadMoreWrap.style.display = '';
  }

  function updateSummaryBars() {
  const total = Number('<%=reviews.length%>');
  const counts = <%-JSON.stringify(count)%>;
  var sumofall=0,counter=0;
  for (let i = 1; i <= 5; i++) {
    document.getElementById('bar' + i).style.width = total ? (counts[i] / total * 100) + '%' : '0%';
    document.getElementById('cnt' + i).textContent = counts[i];
    sumofall+= counts[i]*i;
    counter+=counts[i];
  }
  var avg = counter?(sumofall/counter).toFixed(1):0;
  document.getElementById('reviewCountBadge').textContent = total.toLocaleString('en-IN') + ' Reviews';
  document.getElementById('summaryTotal').textContent = total.toLocaleString('en-IN') + ' ratings';
  document.getElementById('summary-big').textContent=avg;
  document.getElementById('rating-num').textContent=avg;
  document.getElementById('summary-stars').textContent="★".repeat(parseInt(avg));
  document.getElementById('stars-og').textContent="★".repeat(parseInt(avg));


}
setTimeout(updateSummaryBars, 300);
showReviews();

document.getElementById("buy").addEventListener("click",function(){
  const qty = document.getElementById("qtyVal").textContent;
  window.location.href = "/vendrix/directTransaction/buy/"+'<%=spe._id%>'+"/"+'<%=i%>'+"/"+'<%=color_key%>'+'/'+qty;
})
</script>

